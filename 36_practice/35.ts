function makeShirt(size: string, text: string): void {
    console.log(`\n you order a  ${size} shirt that says ${text}`)
    
}
makeShirt(`large`," i love html")
makeShirt(`small`," i need a big shirt")